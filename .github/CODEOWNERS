# Every change needs the maintainer's eyes; these paths are the trust boundary:
# the CLI, the installer, and CI itself. Informational on purpose. The merge
# gate is CI (strict required checks, conversation resolution, no force-push or
# deletion, admins included), matching fschrhunt/e.

*                     @fschrhunt

/bin/                 @fschrhunt
/install.sh           @fschrhunt
/scripts/             @fschrhunt
/.github/             @fschrhunt
