# Workframe 1.5.3

Workframe 1.5.3 is a CLI-only release.

The native macOS menubar app and its Homebrew cask distribution have been
removed. Homebrew users install the `workframe` formula, which provides the
`workframe` and `wf` commands. Existing Workframe stores are independent of
the installation method and remain in the selected store root.
